apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: diagnosis-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      nodePort: 30090
  selector:
    app: diagnosis-app